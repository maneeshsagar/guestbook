services:
  web:
    build: ./web
    expose: ["80"]
    x-clickdep-role: frontend
  api:
    build: ./api
    expose: ["8080"]
    x-clickdep-role: backend
    environment:
      WELCOME_MESSAGE: "Hello from v2"
      LEGACY_FLAG: "will-be-removed-next-deploy"
